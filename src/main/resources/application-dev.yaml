spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/omnidocs
    username: postgres
    password: postgres

  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG

storage:
  location: uploads

jwt:
  secret: 5a2f8b8c9d8e7f6a5b4c3d2e1f0a98765a2f8b8c9d8e7f6a5b4c3d2e1f0a9876

gemini:
  api-key: YOUR_DEV_GEMINI_KEY